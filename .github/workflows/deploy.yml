# Auto-deploy is handled by the iddb webhook function.
# This file is kept as a placeholder — the workflow is disabled.
name: Deploy to iddb (disabled)
on:
  workflow_dispatch:
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploy triggered via iddb webhook function instead"
